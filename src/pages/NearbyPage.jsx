import { useContext, useState } from "react";
import { navigate } from "../router/router";
import { BoardContext } from "../store/BoardContext";
import { useToast } from "../hooks/useToast";
const ITEMS = [{ name:"탐색 핀 근처 식당", emoji:"🍜" },{ name:"골목 작은 카페", emoji:"☕" },{ name:"함께 가기 좋은 전시", emoji:"🖼️" }];
export function NearbyPage({ boardId }) { const { addPlace } = useContext(BoardContext); const [point,setPoint] = useState({ lat:37.56, lon:127.04 }); const [keyword,setKeyword] = useState(""); const [status,setStatus] = useState("results"); const toast=useToast(); const search=()=>setStatus(keyword.includes("오류")?"error":keyword.includes("없음")?"empty":"results"); const add=(item)=>{ if(!addPlace({name:item.name,address:`탐색 좌표 ${point.lat.toFixed(3)}, ${point.lon.toFixed(3)}`,category:"주변 탐색",categoryEmoji:item.emoji,lat:point.lat,lon:point.lon,sourceProvider:"MOCK"})) return toast("이미 모임에 담긴 장소예요."); navigate(`/boards/${boardId}`);};
 return <main className="min-h-screen bg-bg"><section onClick={(e)=>{ const rect=e.currentTarget.getBoundingClientRect(); setPoint({lat:37.50+(e.clientY-rect.top)/rect.height*.1,lon:126.97+(e.clientX-rect.left)/rect.width*.12});}} className="relative h-72 cursor-crosshair bg-[#d7e5df]"><button type="button" onClick={(e)=>{e.stopPropagation();navigate(`/boards/${boardId}/area`)}} className="absolute left-4 top-4 z-10 rounded-xl bg-white p-3">←</button><span className="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 text-3xl">📍</span><p className="absolute bottom-3 left-3 rounded-full bg-white px-3 py-2 text-xs font-bold">탐색 반경 1km · 지도 어디든 눌러 이동</p></section><section className="p-4"><h1 className="font-bold">선택한 좌표 주변 탐색</h1><p className="text-xs text-ink-2">{point.lat.toFixed(4)}, {point.lon.toFixed(4)}</p><div className="mt-3 flex gap-2"><input value={keyword} onChange={(e)=>setKeyword(e.target.value)} className="flex-1 rounded-xl border border-line bg-white p-3" placeholder="카테고리 또는 키워드"/><button type="button" onClick={search} className="rounded-xl bg-navy px-4 font-bold text-white">검색</button></div><div className="mt-3 flex gap-2">{["음식점","카페","놀거리"].map((category)=><button key={category} type="button" onClick={()=>{setKeyword(category);setStatus("results")}} className="rounded-full border border-line bg-white px-3 py-2 text-sm">{category}</button>)}</div>{status==="error"&&<p className="mt-4 rounded-xl bg-white p-4 text-coral">검색에 실패했어요. 다시 시도해 주세요.</p>}{status==="empty"&&<p className="mt-4 rounded-xl bg-white p-4 text-ink-2">결과가 없어요. 다른 위치나 검색어를 써 보세요.</p>}{status==="results"&&<div className="mt-4 space-y-2">{ITEMS.map((item)=><div key={item.name} className="flex items-center gap-3 rounded-xl bg-white p-3"><span>{item.emoji}</span><b className="flex-1">{item.name}</b><button type="button" onClick={()=>add(item)} className="rounded-lg bg-coral px-3 py-2 text-sm font-bold text-white">추가</button></div>)}</div>}</section></main>; }
