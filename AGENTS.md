# frontend

약속 보드 웹 클라이언트. **JavaScript(JSX) 프로젝트.**
API는 별도 배포되는 Spring Boot 서버가 담당한다. 기능·API 계약의 원본은
[`docs`](https://github.com/2026-Siheung-SW-Bootcamp-Team-D/docs) repository의 `specs/`다. (../docs/specs)
문서와 구현이 어긋나면 구현으로 덮어쓰지 말고 먼저 합의한다.

사람이 읽을 실행 방법·디렉토리 구조·배포 절차는 [README.md](README.md)에 있다.

## 없는 것 (있다고 가정하지 말 것)

- **TypeScript**: `tsconfig.json`이 없다. `.ts`/`.tsx`를 추가하지 않는다.
  `@types/react`가 devDependencies에 있으나 타입체크는 돌지 않는다.
- **라우터**: `react-router` 등이 설치되어 있지 않다. 라우팅을 전제로 코드를 쓰지 않는다.
- **테스트 러너**: `npm test`가 존재하지 않는다. 테스트 파일을 임의로 추가하지 않는다.
- **Tailwind 설정 파일**: v4라서 `tailwind.config.js`·`postcss.config.js`가 없고, 만들어도 효과가 없다.
  설정은 `src/index.css`의 `@import 'tailwindcss'`뿐이며 테마 확장은 CSS `@theme` 블록으로 한다.

위 넷을 도입하는 것은 의존성·구조가 늘어나는 결정이므로 먼저 합의한다.

## React 구현 규칙

### 상태와 렌더링

- **다른 state나 props로 계산할 수 있는 값을 state로 만들지 않는다.** 렌더 중에 계산한다.
  두 개의 진실이 생기면 반드시 어긋난다 — 화면 버그의 가장 흔한 원인이다.
- **`useEffect`는 외부 시스템과 동기화할 때만 쓴다**(서버 요청, 타이머, 브라우저 API 구독).
  props·state가 바뀌었다고 다른 state를 갱신하는 effect를 쓰지 않는다. 렌더가 두 번 돌고 깜빡인다.
- **이전 값에 기반한 갱신은 함수형으로 한다**: `setCount(c => c + 1)`.
  `setCount(count + 1)`은 한 이벤트에서 여러 번 호출될 때 마지막 하나만 반영된다.
- **state를 직접 수정하지 않는다.** `arr.push(x)`, `obj.field = v` 대신 새 배열·객체로 교체한다.
  React는 참조가 같으면 바뀌지 않은 것으로 보고 리렌더하지 않는다.
- **개발 모드에서 effect가 두 번 실행되는 것은 정상이다**(StrictMode). 정리 함수(cleanup)로 대응하고,
  거슬린다는 이유로 StrictMode를 끄지 않는다 — 그 두 번째 실행이 잡아 주는 버그가 실제 버그다.

### 훅

- **훅은 컴포넌트 최상위에서만 호출한다.** 조건문·반복문·중첩 함수 안에서 호출하지 않는다.
  React는 호출 순서로 훅을 구분하므로 순서가 바뀌면 다른 상태를 읽는다.
- **의존성 배열 경고를 `eslint-disable`로 덮지 않는다.** 대부분 실제 버그(stale closure)를 가리킨다.
  배열에 넣기 곤란한 값이 있다면 그 값을 effect 밖으로 빼거나 함수형 갱신으로 바꾼다.
- 성능 최적화(`useMemo`, `useCallback`, `memo`)는 **느린 것을 확인한 뒤에** 넣는다.
  기본값으로 감싸면 코드만 복잡해지고 의존성 배열 버그가 늘어난다.

### JSX

- **리스트의 `key`에 배열 인덱스를 쓰지 않는다.** 항목이 추가·삭제·정렬되면 입력값과 상태가
  엉뚱한 행에 남는다. 서버가 준 id처럼 항목에 고정된 값을 쓴다.
- **`{조건 && <컴포넌트 />}`에서 조건이 숫자면 안 된다.** `{items.length && <List />}`는
  길이가 0일 때 화면에 `0`을 그대로 출력한다. `items.length > 0 && ...` 형태로 쓴다.
- **`<form>` 안의 버튼에는 `type="button"`을 명시한다.** 기본값이 `submit`이라 페이지가 새로고침된다.
- 사용자 입력이나 서버 응답을 `dangerouslySetInnerHTML`로 넣지 않는다(XSS).

## API 통신 규칙

- **HTTP 호출은 `src/api/`의 공용 axios 인스턴스를 거친다.** 컴포넌트에서 `axios`를 직접 import 하지 않는다.
  baseURL·인증 헤더·에러 변환을 한 곳에서 바꾸기 위해서다.
- **baseURL은 `import.meta.env.VITE_API_BASE_URL`에서 읽는다.** 서버 주소를 코드에 하드코딩하지 않는다.
- **`VITE_API_BASE_URL`은 `/api/v1`까지 포함한다.** API 모듈은 `/api/v1`을 다시 붙이지 않고 `/boards`처럼 자원 경로만 전달한다.
- **서버 응답을 컴포넌트까지 그대로 흘려보내지 않는다.** `src/api/`에서 화면이 쓸 형태로 한 번 변환한다.
  응답 필드명이 컴포넌트 곳곳에 퍼지면 API가 바뀔 때 전부 고쳐야 한다.
- **요청 결과를 state에 넣기 전에 컴포넌트가 아직 살아 있는지 확인한다.** `AbortController`로 취소하거나
  cleanup에서 무시 플래그를 세운다. 화면을 빠르게 이동하면 사라진 컴포넌트를 갱신하려다 경고가 난다.
- **에러를 삼키지 않는다.** 빈 `catch`나 `console.log`만 남기지 않고 화면에 보여 줄 상태로 바꾼다.
- **API 오류의 내부 정보를 화면에 그대로 출력하지 않는다.** 서버의 `error.code`에 따라 사용자용 문구로 바꾼다.
- **참여 토큰 등 인증 값을 URL 쿼리스트링에 넣지 않는다.** 브라우저 히스토리와 리퍼러에 남는다.

## JavaScript 규칙 (타입체커가 없다)

TypeScript가 없으므로 **오타와 `undefined`는 실행 중에야 드러난다.** 아래 규칙이 그 역할을 대신한다.

- **서버 응답 필드에 접근할 때 옵셔널 체이닝과 기본값을 쓴다**: `res?.data?.items ?? []`.
  응답이 비거나 형태가 다를 때 화면 전체가 흰 화면이 되는 것을 막는다.
- **`==` 대신 `===`를 쓴다.** `0 == ''`이 참이 되는 식의 암묵적 변환을 피한다.
- **함수의 인자와 반환 형태를 중간에 바꾸지 않는다.** 호출부가 깨져도 아무도 알려 주지 않는다.
- 반복되는 문자열·숫자가 의미를 가지면 상수로 뽑는다. 오타가 나도 조용히 통과하기 때문이다.

## 스타일 규칙

- **Tailwind 클래스 이름을 문자열로 조합해 만들지 않는다.** `` `text-${color}-500` `` 같은 코드는
  빌드 시 클래스 스캔에서 걸리지 않아 스타일이 통째로 빠진다. 완성된 클래스명을 조건으로 고른다.
- 전역에 영향을 주는 CSS는 `src/styles/`에만 둔다. 컴포넌트 옆 CSS 파일이 전역 셀렉터를 건드리면
  어디서 깨졌는지 추적할 수 없다.
- 코드에서 `import`하는 이미지는 `src/assets/`, 주소로 직접 접근하는 파일은 `public/`에 둔다.
  `public/`의 파일은 해시가 붙지 않아 캐시가 오래 남는다.

## 환경변수

- `VITE_` 접두사가 붙은 값은 **빌드 결과물에 그대로 박혀 브라우저에 노출된다.** 시크릿을 넣지 않는다.
- 접두사가 없는 변수는 앱 코드에서 읽히지 않는다.
- `process.env`는 동작하지 않는다. `import.meta.env.VITE_*`로 읽는다.
- 커밋되는 환경변수 파일은 `.env.example` 하나다. 변수를 추가·삭제하면 여기도 함께 갱신한다.

## 검증 (코드 수정 후 — 매번)

```bash
npm run lint          # 통과할 때까지 고친다
npm run build         # 커밋·PR 전 1회. 깨지면 완료가 아니다
```

`eslint src/`처럼 **경로를 수동으로 붙이지 않는다.** `dist`는 `eslint.config.js`의 `globalIgnores`에
이미 제외되어 있고, 범위를 좁히면 설정 파일 자체가 검사에서 빠진다.

린트와 빌드는 화면이 깨진 것을 잡지 못한다. UI를 바꿨으면 `npm run dev`로 실제 확인한다.

## 설정 점검 (해당 변경이 있을 때만 — 매번 X)

`eslint.config.js`·`vite.config.js`를 수정하거나 새 산출물 폴더가 생길 때만,
그 경로가 `globalIgnores`와 `.gitignore`에 있는지 확인한다. 그 외에는 생략한다.

## 현재 상태 (2026-07 기준)

`src/App.jsx`, `src/App.css`, `src/assets/*`는 Vite 스타터 템플릿이다. 참고 대상이 아니라 교체 대상이며,
실제 화면 작업이 시작되면 이 항목은 삭제한다.
